넓이x 높이x 요소의 개수(바이트)

1개의 픽셀이 1비트니까 (1비트=1/8바이트) 요소의 개수에 /8 (= *1/8)을 해준다.



1비트 = 1/8바이트  (8비트 = 1바이트)



24비트 / 8(비트) = 3비트


24비트 = 3바이트


Q. 1비트로 된 이미지를 24비트로 변경해라

Q. 4비트로 된 이미지를 24비트로 변경해라

Q. 8비트로 된 이미지를 24비트로 변경해라


환경 변수 용어?
